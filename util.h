#ifndef __UTIL_H
#define __UTIL_H

#include <stdio.h>
#include <string.h>

void binary_to_hex(char *, const char *, unsigned int);
void hex_to_binary(char *, const char *, unsigned int);
char hc2int (char);
void reverse(char *, const char *, unsigned int);

#endif // __UTIL_H
